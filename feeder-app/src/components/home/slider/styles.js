import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerMain: {
    // flex: 1,
    padding: 20,
    alignItems: "center",
  },
  titleStyle: {
    fontWeight: "bold",
    fontSize: 30,
  },
});

export default styles;
